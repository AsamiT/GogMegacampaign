country_decisions = {

	polish_nation = {
		major = yes
		potential = {
			NOT = { has_country_flag = formed_poland_flag }
			OR = {
				ai = no
			}
			NOT = { tag = GER }
			OR = {
				NOT = { exists = POL }
				ai = no
			}
			NOT = { tag = PAP }
			NOT = { tag = HLR }
			OR = {
				primary_culture = polish
			}
			OR = {
				ai = no
				AND = {
					ai = yes
					num_of_cities = 3
				}
			}
			is_colonial_nation = no
			OR = {
				is_former_colonial_nation = no
				AND = {
					is_former_colonial_nation = yes
					ai = no
				}
			}
		}
		allow = {
			NOT = { exists = POL }
			adm_tech = 4
			is_free_or_tributary_trigger = yes
			is_nomad = no
			is_at_war = no
			owns_core_province = 262			# Krakow
			owns_core_province = 257			# Warszawa
			OR = {
				owns_core_province = 2424		# Premyzhl
				owns_core_province = 269		# Podlaise			
		}
		effect = {
			change_tag = POL
			if = {
				limit = {
					is_part_of_hre = yes
					is_elector = no
					is_emperor = no
				}
				every_owned_province = {
					limit = {
						is_part_of_hre = yes
					}
					set_in_empire = no
				}
			}
			set_government_and_rank = {
					government = elective_monarchy
					rank = 2
			}
			poland_region = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = POL
			}
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			country_event = { id = ideagroups.1 } #Swap Ideas
			set_country_flag = formed_poland_flag
			add_prestige = 25
		}
		ai_will_do = {
			factor = 1
		}
		ai_importance = 400
	}
	
}
